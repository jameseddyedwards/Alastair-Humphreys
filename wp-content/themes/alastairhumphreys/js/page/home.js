(function($) {

	/*
	var selector = $('#carousel');

	function initCarousel() {
		selector.carouFredSel({
			items : {
				width : 1440,
				height : 'auto'
			},
			next : {
				button : "#next"
			},
			prev : {
				button : "#previous"
			},
			responsive  : true,
			scroll: {
				fx: 'crossfade',
				duration: 300,
				timeoutDuration: 5000,
				pauseOnHover: true
			},
			width : '100%'
		});
	}

	if (selector.length > 0) {
		selector.imagesLoaded(initCarousel);
	}
	*/

})(jQuery);

